#ifndef _BITMAP_H
#define _BITMAP_H

void setBit(int* array, int bitToSet);
void initBM(int* array, int bmSize);
void printBits(int n);
void clearBit(int* array, int bitToClear);
int checkBit(int* array, int bitToCheck);

#endif